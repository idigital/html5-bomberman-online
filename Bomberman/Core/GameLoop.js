(function( window ) {
    /**
     * Class that defines the game loop 
     */
    Bomberman.GameLoop = Class.create({
        /**
         * Constrcutor
         */
        initialize : function()
        {
            
        }
    });
})( window )